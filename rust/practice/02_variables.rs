fn main1() {
  // This is a mutable variable. Without `mut` we cannot change the
  // variable value afterwards.
  let mut x = 5;
  x = x * 2;
  println!("x value: {x}");
}

fn main() {
  let x = 5;

  // This is variable 
  let x = x + 1;

  
}
