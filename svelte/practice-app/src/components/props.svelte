<script lang="ts">
	// This is a prop with a default value of 20.
	export let someProp: number = 20;

	// We can also use `$$props` to access all props.
</script>

<p>This component is taking a prop someProp = {someProp}</p>
